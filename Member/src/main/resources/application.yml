spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/auth_test?useSSL=false&useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379

  security:
    oauth2:
      kakao:
        issuer: "https://kauth.kakao.com"
        rest-api-key: 3d9ba27e79e64cf857e8e91bbfc88346
      apple:
        issuer: "https://appleid.apple.com"
        client-id: umc.MEME

jwt:
  issuer: meme_umc
  secret-key:
  access-token-validity-in-seconds: 1800  # 30 minutes
  refresh-token-validity-in-seconds: 86400  # 24 hours

springdoc:
  swagger-ui:
    path: /auth/swagger-ui
    display-request-duration: true
  api-docs:
    path: /auth/docs